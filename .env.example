APP_HOST=0.0.0.0
APP_PORT=8091
APP_ENV=development
LOG_LEVEL=info

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=session_manager_service
DB_SSLMODE=disable

# --- Инфраструктура (опционально) ---
# Kafka (общий брокер из infra/; события о создании/изменении сессий)
# KAFKA_BROKERS=localhost:9092
